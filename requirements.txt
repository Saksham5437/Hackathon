# ─────────────────────────────────────────────────────────
# Mini NotebookLM — Python Dependencies
# Install with: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────

# Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # Required for FastAPI file uploads

# Config
python-dotenv>=1.0.0

# Document parsing
pypdf>=4.2.0
python-docx>=1.1.0
python-pptx>=1.0.0          # PPTX slide extraction

# Text-to-speech
gTTS>=2.5.0                 # Google Text-to-Speech (offline MP3 generation)

# Embeddings & Vector Store
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0
langchain-chroma>=0.1.0
langchain-huggingface>=0.0.3
chromadb>=0.5.0
sentence-transformers>=2.7.0  # Powers all-MiniLM-L6-v2

# LLM Providers (install the one(s) you need)
google-generativeai>=0.7.0    # For Gemini
openai>=1.30.0                # For OpenAI GPT (optional)

# Utilities
pydantic>=2.7.0

# HTTP client (used for YouTube Data API v3)
requests>=2.32.0

# Security
passlib[bcrypt]>=1.7.4
